class Parameters {
	constructor() {}

	addEdgesMap(edgesMap, edgeColourMap) {this.edgesMap = edgesMap; this.edgeColourMap=edgeColourMap}

	addTrainLineCapacity(capacityMap) {this.capacityMap = capacityMap}

	addStationSpawnScale(stationSpawnScale) {this.stationSpawnScale = stationSpawnScale}

	addTrainLinePeriod(linePeriodMap) {this.linePeriodMap = linePeriodMap}
}